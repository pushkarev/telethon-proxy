# 2026-03-23

- Added `telegram-project/list_mcp_chats.py`, a small stdlib-based MCP client that reads local proxy config, initializes an MCP session, calls `telegram.list_chats`, and prints chats as TSV or JSON.
- Added `telegram-project/tests/test_mcp_client.py` to verify the client against the in-process MCP server.
- Verified the client live against the local MCP endpoint on `127.0.0.1:8791/mcp`; it returned chats successfully.
- Fixed Cloud dialog limiting in `telegram-project/telegram_proxy/upstream.py` so limits are applied after Cloud-folder filtering instead of before.
- Added `telegram-project/echo_mcp_chats.py`, a polling MCP monitor that watches all Cloud chats via `telegram.get_updates` and echoes new incoming messages back with `telegram.send_message`.
- Extracted the dashboard UI into `telegram-project/webui/` and added an Electron desktop shell in `electron/main.mjs` that reuses an already-running local proxy dashboard when present, or starts `telegram-project/proxy_service.py` itself when needed.
- Added macOS packaging: `scripts/build-python-service.sh` bundles the Python service with PyInstaller, `electron-builder` packages the Electron shell, and `npm run app:dist` now produces a `.app`, `.zip`, and `.dmg` under `dist/electron/`.
- Split Telegram auth deletion into two UI paths: `Delete session` now removes only the saved Telegram session and clears live chat access while keeping API keys, while `Clear all saved auth` wipes both keys and session from Keychain; verified live on the `127.0.0.1:8788` service and pushed the UI back into the code-request step for relogin.
- User asked that substantial changes be committed after they are made; treat that as the default workflow preference for this workspace.
- User prefers separate branches and worktrees per feature thread for parallel Codex work; organized the repo into `codex/integration-wip`, `codex/telegram-auth-desktop`, and `codex/whatsapp-mcp`, with a `THREADS.md` map.
- Added `telegram_proxy/secrets_store.py` as the shared secret manager for Telegram credentials/session and the MCP bearer token, migrated the MCP token out of `~/.tlt-proxy/mcp_token` into macOS Keychain on service restart, and removed file-based secret persistence for that token.
- Added `telegram-project/echo_mcp_whatsapp.py`, a simple polling MCP client that watches `whatsapp.get_updates`, marks incoming messages read with `whatsapp.mark_read`, and echoes them back with `whatsapp.send_message`.
- Added `telegram-project/tests/test_mcp_whatsapp_echo.py` plus `telegram-project/tests/test_whatsapp_bridge.py` for the WhatsApp echo helper logic and launchd-safe Node resolution.
- Fixed `telegram-project/telegram_proxy/whatsapp_bridge.py` so the launchd-managed service can find Node via common Homebrew paths instead of crashing with `FileNotFoundError: node`.
- Verified the live MCP endpoint now serves `whatsapp.get_auth_status`; current state is `connected=True` but `cloud_label_found=False`, so the WhatsApp echo client starts cleanly and waits with no chats until a `Cloud` label exists.
- Fixed the WhatsApp bridge blank-chat state in `whatsapp-project/service.mjs` by falling back to persisted `lid-mapping-*.json` peers when Baileys reconnects without repopulating chat history; after restarting launchd, the live bridge and MCP surface both exposed chats again even with `cloud_label_found=False`.
- Tightened the Electron desktop shell and dashboard UI: smaller default window, more concise header, removed the Configuration "Connected clients" section, and reworked WhatsApp into Telegram-style `Chats` and `Settings` panes.
- Committed the UI pass as `3293a3d` (`Tighten desktop UI and align WhatsApp layout`) on `dev` and fast-forwarded `master` to the same commit.
