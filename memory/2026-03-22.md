# 2026-03-22

- User explicitly approved using local `doppler` without asking for confirmation first.
- Treat `doppler` as pre-approved for internal workspace tasks like reading configs, fetching secrets, and running local commands with injected env.
- Still avoid writing secret values into repo files or printing them back unless the user asks.
- User also approved skipping chat confirmations for safe system calls that are necessary to complete the current task.
- Treat routine task-critical commands as pre-approved on the assistant side, while still avoiding destructive actions without explicit user direction.
- User later changed this preference and now wants confirmation before running local commands needed to move development forward.
- Added `telegram-project/list_proxy_chats.py`, a plain Telethon client for proxy-issued sessions that can override the session endpoint host/port and list dialogs.
- Verified the client against the proxy endpoint at `100.92.237.54:9001`; it authenticated as `Proxy User` and listed chats successfully.
- Expanded `telegram-project/list_proxy_chats.py` into a long-running listener: it now lists chats on startup, watches incoming messages across accessible chats, delivers each message to an optional shell hook from `TP_INCOMING_HOOK`, and echoes the message back into the same chat.
- Fixed read-ack handling: the listener now calls `send_read_acknowledge` for each incoming message, and the proxy MTProto dispatcher now accepts `channels.ReadHistoryRequest` in addition to `messages.ReadHistoryRequest`.
- Investigated background Python resource usage: the expected idle process is `telegram-project/proxy_service.py` under the `dev.telethon-proxy` launch agent, while a detached `python -m unittest discover -s tests -v` runner was traced to a prior Codex session started on 2026-03-22 at 08:27:58 local time and had to be killed manually after it became orphaned under `launchd`/PPID 1.
- Added a README troubleshooting note: if a stray detached unittest runner is discovered consuming resources, inspect `python|proxy_service|unittest` processes and kill only the unittest PID, not the managed proxy service.
