- OpenClaw web search was configured to use Perplexity with a working API key; live `web_search` calls returned `provider: perplexity` successfully.
- Remote Control UI is working over Tailscale Serve at https://claw.taild0940.ts.net ; direct raw tailnet IP access on port 18789 was not switched over and remains loopback-backed.
- WhatsApp channel/plugin was enabled and linked successfully; system later reported the WhatsApp gateway connected as +16506448988.
- A usage dashboard service was built in `dashboard/usage_app.py` to reconstruct OpenAI spend from saved session transcripts and Perplexity request history from `web_search` tool results. It runs locally at `http://127.0.0.1:8789/usage-dashboard/`.
- The dashboard code was committed in workspace git as commit `42e8a44` with message `Add OpenClaw usage dashboard service`.
- Publishing the usage dashboard at `https://claw.taild0940.ts.net/usage-dashboard/` is blocked by Tailscale Serve requiring root on this host. User needs to run: `sudo tailscale serve --bg --set-path /usage-dashboard 8789`.
